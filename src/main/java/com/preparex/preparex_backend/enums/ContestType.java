package com.preparex.preparex_backend.enums;

/**
 * Contest type — determines marking scheme and result calculation strategy.
 */
public enum ContestType {
    JEE_MAINS_MOCK,
    JEE_ADVANCED_MOCK,
    NEET_MOCK,
    SUBJECT_WISE
}
